apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-stats-measurements-chart.postgresName" . }}
spec:
  selector:
    app: {{ include "ai-stats-measurements-chart.postgresName" . }}
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: 5432
  type: ClusterIP